# No models yet. `core` is scaffolding (home page + healthz) for M0.
#
# Shared-zone and private-zone tables (firms, opportunities, contacts,
# touches, ...) belong to coverage_domain per docs/build-plan.md §2 — they do
# not live in this app.
